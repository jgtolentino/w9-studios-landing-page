# Google API Configuration
# Get these from Google Cloud Console: https://console.cloud.google.com

# OAuth 2.0 Client ID (public, safe to expose in frontend)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# OAuth 2.0 Client Secret (keep secure, server-side only)
GOOGLE_CLIENT_SECRET=GOCSPX-your-client-secret

# Redirect URI (must match Google Cloud Console settings)
NEXT_PUBLIC_GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback

# Refresh Token (obtained after first OAuth flow)
# Run the OAuth flow at http://localhost:3000/admin/setup to get this
GOOGLE_REFRESH_TOKEN=

# Optional: Supabase credentials (for future database integration)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=